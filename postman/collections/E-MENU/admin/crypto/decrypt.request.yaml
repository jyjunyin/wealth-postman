$kind: http-request
url: "{{$baseURL}}/{{$contextPath}}/api/v1/admin/crypto/decrypt"
method: POST
headers:
  Content-Type: application/json
  Authorization: Bearer {{$system_access_token}}
body:
  type: text
  content: |-
    {
        "cipher_text": "{{$value_encrypted}}"
    }
order: 2000
